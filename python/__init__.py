"""Top‑level Python package for the Shattered Pixel Dungeon AI project.

This package exposes submodules such as `env`, `train` and `models`.  It
allows running training scripts with `python -m python.train.train_rnd` etc.
"""
